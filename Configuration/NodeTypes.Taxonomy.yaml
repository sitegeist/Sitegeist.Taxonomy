Sitegeist.Taxonomy:Taxonomy:
  label: "${String.stripTags(q(node).property('title') + ': ' + q(node).property('_path'))}"
  ui:
    label: i18n
    icon: 'icon-tag'
  superTypes:
    'Neos.Neos:Node': TRUE
  constraints:
    nodeTypes:
      '*': FALSE
      'Sitegeist.Taxonomy:Taxonomy': TRUE
  properties:
    title:
      type: string
    description:
      type: string
